{
  "name": "hono-template-new",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && vite build --mode client",
    "preview": "pnpm build && vite preview",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "hono": "^4.7.5"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@cloudflare/vite-plugin": "^0.1.15",
    "@cloudflare/workers-types": "^4.20250214.0",
    "vite": "^6.1.1",
    "vite-plugin-ssr-hot-reload": "^0.2.2",
    "wrangler": "^4.4.0"
  }
}
